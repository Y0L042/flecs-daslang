-- When included from the Duin workspace, all FLECS_DAS_* variables and workspace-level
-- settings (configurations, outputdir, staticruntime) are already set by the root premake.
-- This file only includes the sub-projects.

include "flecs_das"
include "flecs_das_tests"
